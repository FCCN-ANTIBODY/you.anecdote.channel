#!/bin/zsh
# test — every *.test.mjs here, as standalone Node scripts that exit non-zero on failure (the house style).
emulate -L zsh; set -uo pipefail; cd "${0:A:h:h}"
rc=0
for t in bottle/*.test.mjs; do print "── $t"; node "$t" || rc=1; done
exit $rc
