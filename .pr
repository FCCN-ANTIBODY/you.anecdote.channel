bottles: keep _data/ out of the consumer's file list

The README reads it (site.data.control drives the greeting; proven by a local build); a person has nowhere to go in it. Still in the bottle, still routable by hash, just not listed.
